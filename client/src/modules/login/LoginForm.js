const { default: Button } = require('modules/common/Button');
const { default: Input } = require('modules/common/Input');

function LoginForm() {
  return (
    <form>
      <Input />
      <Input />
      <Button />
    </form>
  );
}
